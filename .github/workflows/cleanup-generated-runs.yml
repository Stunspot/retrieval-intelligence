name: Clean failed Pages and artwork runs

on:
  push:
    paths:
      - .github/workflows/cleanup-generated-runs.yml

permissions:
  actions: write
  contents: write

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Delete only failed runs created by this Pages overhaul
        id: cleanup
        env:
          GH_TOKEN: ${{ github.token }}
          CURRENT_RUN_ID: ${{ github.run_id }}
          REPOSITORY: ${{ github.repository }}
        run: |
          python - <<'PY'
          import json
          import os
          from urllib.request import Request, urlopen

          token = os.environ['GH_TOKEN']
          repo = os.environ['REPOSITORY']
          current = str(os.environ['CURRENT_RUN_ID'])
          cutoff = '2026-07-25T00:00:00Z'
          bad = {'failure', 'cancelled', 'timed_out', 'action_required', 'startup_failure', 'stale'}
          path_markers = (
              '.github/workflows/deploy-pages.yml',
              '.github/workflows/capture-',
              '.github/workflows/generate-',
              '.github/workflows/inspect-',
              '.github/workflows/publish-',
          )
          name_markers = (
              'Deploy GitHub Pages',
              'Generate ',
              'Inspect ',
              'Publish canonical ',
          )
          headers = {
              'Authorization': f'Bearer {token}',
              'Accept': 'application/vnd.github+json',
              'X-GitHub-Api-Version': '2022-11-28',
              'User-Agent': 'pages-overhaul-run-cleaner',
          }

          deleted = []
          page = 1
          while True:
              req = Request(
                  f'https://api.github.com/repos/{repo}/actions/runs?per_page=100&page={page}',
                  headers=headers,
              )
              with urlopen(req, timeout=60) as response:
                  payload = json.load(response)
              runs = payload.get('workflow_runs', [])
              if not runs:
                  break
              for run in runs:
                  run_id = str(run.get('id'))
                  if run_id == current or run.get('status') != 'completed':
                      continue
                  if (run.get('created_at') or '') < cutoff:
                      continue
                  conclusion = run.get('conclusion')
                  path = run.get('path') or ''
                  name = run.get('name') or ''
                  ours = any(marker in path for marker in path_markers) or any(name.startswith(marker) for marker in name_markers)
                  if conclusion in bad and ours:
                      delete = Request(
                          f'https://api.github.com/repos/{repo}/actions/runs/{run_id}',
                          headers=headers,
                          method='DELETE',
                      )
                      with urlopen(delete, timeout=60) as response:
                          if response.status != 204:
                              raise RuntimeError(f'Unexpected delete status {response.status} for run {run_id}')
                      deleted.append((run_id, name, conclusion, path))
              if len(runs) < 100:
                  break
              page += 1

          print(f'Deleted {len(deleted)} failed/cancelled overhaul run(s).')
          for record in deleted:
              print('deleted', *record)
          with open(os.environ['GITHUB_OUTPUT'], 'a', encoding='utf-8') as output:
              output.write(f'deleted={len(deleted)}\n')
          PY
      - name: Remove the one-shot cleanup workflow
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/cleanup-generated-runs.yml
          git commit -m "chore: remove Actions cleanup after deleting ${{ steps.cleanup.outputs.deleted }} failed run(s)"
          git push
